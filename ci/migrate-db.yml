platform: linux

image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
- name: src

params:
  app: ((app))
  name: ((name))
  cmd: ((cmd))
  opts: ((opts))

run:
  path: src/ci/migrate-db.sh
