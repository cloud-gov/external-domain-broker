platform: linux

image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
- name: src

params:
  app: ((app))
  service: ((service))
  plan: ((plan))

run:
  path: src/ci/create-db.sh
