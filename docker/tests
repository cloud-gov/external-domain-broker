#!/usr/bin/env bash

cd /app || (echo "Cannot find /app directory"; exit 2)
set -x

./docker/start-redis.sh

export FLASK_ENV=test

if [[ "$#" -ge 1 ]] && [[ "$1" == "watch" ]]; then
  shift
  ptw --spool=500 -- -v "$@"
else
  python -m pytest -v "$@"
fi

./docker/stop-redis.sh
